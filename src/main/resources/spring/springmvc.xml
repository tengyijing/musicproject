<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
        http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd">

	<!-- 配置注解驱动 -->
	<mvc:annotation-driven>
		<!-- 消息转换器,解决responseBody返回中外乱码问题 -->
	   <mvc:message-converters register-defaults="true">
	      <bean class="org.springframework.http.converter.StringHttpMessageConverter">
	         <property name="supportedMediaTypes" value="text/plain;charset=UTF-8" />
	      </bean>
	   </mvc:message-converters>
	</mvc:annotation-driven>
	
	<!--设置拦截器-->
    <mvc:interceptors>
        <mvc:interceptor>
            <!--需要拦截的controller连接-->
            <mvc:mapping path="/User/changUserInfo"/>
            <mvc:mapping path="/User/upHeadImage"/>
            <mvc:mapping path="/User/userCityInfo"/>
            <mvc:mapping path="/User/allProvinceInfo"/>
            <mvc:mapping path="/User/CityInfoById"/>
            <mvc:mapping path="/User/personalInfo"/>
            <mvc:mapping path="/mysong"/>
            <mvc:mapping path="/singer"/>
            <mvc:mapping path="huati"/>
            <bean class="cn.qst.intercept.LoginInterceptor"></bean>
        </mvc:interceptor>
    </mvc:interceptors>
	
	<!-- 加载文件服务器的配置文件 -->
	<context:property-placeholder location="classpath:fastdfs/resource.properties"/>
	<!-- 定义文件上传解析器 -->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
	    <!-- 设定默认编码 -->
	    <property name="defaultEncoding" value="UTF-8"></property>
	    <!-- 设定文件上传的最大值为5MB，5*1024*1024 -->
	    <property name="maxUploadSize" value="5242880"></property>
	    <!-- 设定文件上传时写入内存的最大值，如果小于这个参数不会生成临时文件，默认为10240 -->
	    <property name="maxInMemorySize" value="40960"></property>
	    <!-- 延迟文件解析 -->
	    <property name="resolveLazily" value="true"/>
	</bean>
	<!-- 视图解析器 -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/jsp/"/>
		<property name="suffix" value=".jsp" />
	</bean>
	<!-- 配置包扫描器，扫描@Controller注解的类 -->
	<context:component-scan base-package="cn.qst.controller"/>
	<!-- 配置资源映射 -->
	<!--<mvc:resources location="/css/" mapping="/css/**"/>-->
	<mvc:resources location="/myjs/" mapping="/myjs/**"/>
	<mvc:resources mapping="/source/**" location="/source/"/>
	<mvc:resources location="/adminstyle/" mapping="/adminstyle/**"/>
	<mvc:resources location="/playmusic/" mapping="/playmusic/**"/>
	<mvc:resources location="/images/" mapping="/images/**"/>

	<!-- 引用dubbo服务 -->
	<!-- <dubbo:application name="taotao-manager-web"/>
	<dubbo:registry protocol="zookeeper" address="192.168.25.167:2181"/>	
	<dubbo:reference interface="com.taotao.service.ItemService" id="itemService" /> -->
</beans>